// This script can be placed anywhere in your project (outside the Editor folder).
using UnityEngine;

public class ReadOnlyAttribute : PropertyAttribute
{
    // This attribute is empty because it's just a tag.
    // It doesn't need to store any data.
}
